---
apiVersion: v1
kind: ConfigMap
metadata:
  name: git-config
data:
  # Git user configuration (optional)
  git-user-name: ""
  git-user-email: ""

  # Git authentication (optional - references to secret names)
  git-ssh-key-secret: ""
  git-token-secret: ""

  # Default repositories to clone (optional)
  # One repository URL per line
  git-repositories: |

  # Global Git configuration options
  git-clone-on-startup: "false"
  git-workspace-path: "/workspace/git-repos"
---
# Example configmap with sample values (commented out)
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: git-config
#   namespace: ambient-agentic
# data:
#   git-user-name: "vTeam Bot"
#   git-user-email: "vteam@example.com"
#   git-ssh-key-secret: "git-ssh-key"
#   git-token-secret: "git-token"
#   git-repositories: |
#     https://github.com/organization/project.git
#     https://github.com/organization/docs.git
#     https://github.com/organization/config.git
#   git-clone-on-startup: "true"
#   git-workspace-path: "/workspace/git-repos"
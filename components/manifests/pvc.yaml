apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vteam-state-storage
  labels:
    app: vteam
    component: state-storage
spec:
  accessModes:
    #- ReadWriteMany  # Prefer RWX if available
    - ReadWriteOnce   # Fallback if RWX not supported
  resources:
    requests:
      storage: 10Gi

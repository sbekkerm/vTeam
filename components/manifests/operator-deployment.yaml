apiVersion: apps/v1
kind: Deployment
metadata:
  name: research-operator
  namespace: ambient-code-research
  labels:
    app: research-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: research-operator
  template:
    metadata:
      labels:
        app: research-operator
    spec:
      serviceAccountName: research-operator
      containers:
      - name: research-operator
        image: quay.io/ambient_code/operator:latest
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: BACKEND_API_URL
          value: "http://backend-service:8080/api"
        - name: AMBIENT_CODE_RUNNER_IMAGE
          value: "quay.io/ambient_code/claude-code-runner:latest"
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "ps aux | grep '[o]perator' || exit 1"
          initialDelaySeconds: 30
          periodSeconds: 10
      restartPolicy: Always
